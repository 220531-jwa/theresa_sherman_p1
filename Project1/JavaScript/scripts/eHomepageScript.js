function getMyRequests(){
}

let requestDiv = document.getElementById("requests");

let nameHeader = document.createElement('h3');
nameHeader.innterHTML = client1.name;

clientDiv.append(nameHeader);

// clientsJSON = JSON.stringify(requests);

// requestsDiv.innerHTML = requestsJSON;k